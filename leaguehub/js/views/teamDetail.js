/**
 * Vista: Detalle de Equipo (#team/:id)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.teamDetail(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.teamDetail = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Detalle de Equipo</h2>
      <p>Vista pendiente de implementar (ruta: <code>#team/:id</code>).</p>
    </div>
  `;
};

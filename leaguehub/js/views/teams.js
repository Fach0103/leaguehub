/**
 * Vista: Equipos (#teams)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.teams(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.teams = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Equipos</h2>
      <p>Vista pendiente de implementar (ruta: <code>#teams</code>).</p>
    </div>
  `;
};

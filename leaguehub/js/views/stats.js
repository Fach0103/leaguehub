/**
 * Vista: Estadísticas (#stats)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.stats(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.stats = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Estadísticas</h2>
      <p>Vista pendiente de implementar (ruta: <code>#stats</code>).</p>
    </div>
  `;
};

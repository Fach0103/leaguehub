/**
 * Vista: Detalle de Partido (#match/:id)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.matchDetail(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.matchDetail = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Detalle de Partido</h2>
      <p>Vista pendiente de implementar (ruta: <code>#match/:id</code>).</p>
    </div>
  `;
};

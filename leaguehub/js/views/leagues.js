/**
 * Vista: Ligas (#leagues)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.leagues(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.leagues = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Ligas</h2>
      <p>Vista pendiente de implementar (ruta: <code>#leagues</code>).</p>
    </div>
  `;
};

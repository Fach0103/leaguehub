/**
 * Vista: Partidos (#matches)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.matches(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.matches = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Partidos</h2>
      <p>Vista pendiente de implementar (ruta: <code>#matches</code>).</p>
    </div>
  `;
};

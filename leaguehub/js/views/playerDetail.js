/**
 * Vista: Detalle de Jugador (#player/:id)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.playerDetail(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.playerDetail = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Detalle de Jugador</h2>
      <p>Vista pendiente de implementar (ruta: <code>#player/:id</code>).</p>
    </div>
  `;
};

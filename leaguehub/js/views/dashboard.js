/**
 * Vista: Dashboard (#dashboard)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.dashboard(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.dashboard = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Dashboard</h2>
      <p>Vista pendiente de implementar (ruta: <code>#dashboard</code>).</p>
    </div>
  `;
};

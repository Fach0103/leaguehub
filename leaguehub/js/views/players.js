/**
 * Vista: Jugadores (#players)
 * Placeholder de Fase 0 — se reemplaza en su fase correspondiente.
 * Contrato: LH.views.players(root, params) -> pinta dentro de root.
 */
window.LH = window.LH || {};
LH.views = LH.views || {};

LH.views.players = function (root, params) {
  root.innerHTML = `
    <div class="view-empty">
      <h2>Jugadores</h2>
      <p>Vista pendiente de implementar (ruta: <code>#players</code>).</p>
    </div>
  `;
};
